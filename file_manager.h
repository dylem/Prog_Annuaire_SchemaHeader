
#ifndef FILE_MANAGER
#define FILE_MANAGER

#include "student_manager.h"

  int load_file(int size, Student *ptr_name, Student *ptr_phone, char filename[]);
  int save_file(int size, Student *ptr_name, char filename[]);

#endif
