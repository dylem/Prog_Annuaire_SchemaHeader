#ifndef HASH_MANAGER
#define HASH_MANAGER

int hashing(size, Student **ptr_name, name);
int hashing(size, Student **ptr_phone, phone);
int collision(size, Student **ptr_name, name);
int collision(size, Student **ptr_phone, phone);

#endif
